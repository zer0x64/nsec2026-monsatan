// This app is a pure client-side SPA served via a static fallback.
// Disable SSR and prerendering so all routing is handled in the browser.
export const ssr = false;
export const prerender = false;
